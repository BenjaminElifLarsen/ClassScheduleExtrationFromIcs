namespace ClassScheduleExtrationFromIcs.Schedule;

internal class CoursePart
{ //course part as it for each day, each block of lecture time

    private HashSet<string> _classes;
    private string _lectureName;
    private DateTime _start;
    private DateTime _end;
}
